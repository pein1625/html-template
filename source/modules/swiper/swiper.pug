mixin preview-slider
  .preview-slider
    .preview-slider__container.swiper-container
      .swiper-wrapper
        - for(let i = 0; i < 6; i++)
          .swiper-slide.bg-white
            a.ratio.ratio-1x1(href=`http://placekitten.com/1000/75${i}`, data-fancybox="preview")
              img(src=`http://placekitten.com/1000/75${i}`, alt="")

mixin thumb-slider
  .thumb-slider
    .thumb-slider__prev
      i.fa.fa-angle-left
    .thumb-slider__next
      i.fa.fa-angle-right
    .thumb-slider__container.swiper-container
      .swiper-wrapper
        - for(let i = 0; i < 6; i++)
          .swiper-slide.bg-white
            .ratio.ratio-1x1
              img(src=`http://placekitten.com/1000/75${i}`, alt="")

mixin preview
  article.preview
    .preview__main
      +preview-slider
    .preview__thumbs
      +thumb-slider

mixin swiper(el, number, hasNav, hasPaging)
  div(class=el)
    if (hasNav)
      div(class=el + '__prev')
        i.fa.fa-angle-left
      div(class=el + '__next')
        i.fa.fa-angle-right
    if (hasPaging)
      div(class=el + '__pagination')
    .swiper-container(class=el + '__container')
      .swiper-wrapper
        - for(let i = 0; i < number; i++)
          .swiper-slide
            block

mixin banner-slider(addClass='', hasNav = 0)
  .banner-slider(class=addClass)
    if (hasNav)
      .banner-slider__prev
        i.fal.fa-arrow-left
      .banner-slider__next
        i.fal.fa-arrow-right
    .banner-slider__pagination
    .banner-slider__container.swiper-container
      .swiper-wrapper
        - for(let i = 0; i < 4; i++)
          .swiper-slide
            if (block)
              block
            else
              a.banner-slider__frame(href="#!")
                img(src="./images/banner-slide.jpg", alt="")

mixin partner-slider
  .partner-slider
    .partner-slider__prev
      i.fa.fa-angle-left
    .partner-slider__next
      i.fa.fa-angle-right
    .partner-slider__container.swiper-container
      .swiper-wrapper
        - for(let i = 0; i < 6; i++)
          .swiper-slide
            a.partner-slider__frame(href="#!")
              //- img(src=`./images/partner-${i + 1}.png`, alt="")
              +img

mixin card-slider
  .card-slider
    .card-slider__pagination
    .card-slider__container.swiper-container
      .swiper-wrapper
        - for(let i = 0; i < 6; i++)
          .swiper-slide
            +card

mixin card-slider-2
  .card-slider-2
    .card-slider-2__pagination
    .card-slider-2__container.swiper-container
      .swiper-wrapper
        - for(let i = 0; i < 6; i++)
          .swiper-slide
            +card

mixin card-slider-3(img='./images/card-slider-3.png')
  .card-slider-3
    .card-slider-3__pagination
    .card-slider-3__container.swiper-container
      .swiper-wrapper
        - for(let i = 0; i < 6; i++)
          .swiper-slide
            a.card-slider-3__frame(href="#!")
              img(src=img, alt="")

mixin shop-slider
  .shop-slider
    .shop-slider__prev
      i.fal.fa-angle-left
    .shop-slider__next
      i.fal.fa-angle-right
    .shop-slider__container.swiper-container
      .swiper-wrapper
        - for(let i = 0; i < 8; i++)
          .swiper-slide
            +card(`./images/shop-${i % 6 + 1}.jpg`, '#!', 'card--square')

mixin partner-slider
  .partner-slider
    .partner-slider__prev
      i.fa.fa-angle-left
    .partner-slider__next
      i.fa.fa-angle-right
    .partner-slider__container.swiper-container
      .swiper-wrapper
        - for(let i = 0; i < 6; i++)
          .swiper-slide
            a.partner-slider__frame(href="#!")
              //- img(src=`./images/partner-${i + 1}.png`, alt="")
              +img

mixin product-slider
  .product-slider
    .product-slider__prev
      i.fal.fa-angle-left
    .product-slider__next
      i.fal.fa-angle-right
    .product-slider__container.swiper-container
      .swiper-wrapper
        - for(let i = 0; i < 8; i++)
          .swiper-slide
            +product-2

mixin product-slider-2
  .product-slider-2
    .product-slider-2__prev
      i.fal.fa-angle-left
    .product-slider-2__next
      i.fal.fa-angle-right
    .product-slider-2__container.swiper-container
      .swiper-wrapper
        - for(let i = 0; i < 8; i++)
          .swiper-slide
            +product-2
